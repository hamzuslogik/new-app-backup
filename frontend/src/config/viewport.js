/** Largeur layout forcée sur mobile (Android / iOS) — affichage desktop avec scroll horizontal */
export const DESKTOP_VIEWPORT_WIDTH = 1400;

/** Toujours true : toute l'app utilise le rendu desktop sur téléphone et tablette */
export const FORCE_DESKTOP_VIEWPORT = true;
