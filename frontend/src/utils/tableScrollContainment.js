/**
 * Scroll des tableaux : propagation vers la page (plus de blocage touch iOS).
 * Voir overscroll-behavior: auto sur les conteneurs dans desktop-viewport.css.
 */
export function initTableScrollContainment() {
  /* no-op */
}
